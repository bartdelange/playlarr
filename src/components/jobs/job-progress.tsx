"use client";
import { useEffect, useState } from "react"; import type { StoredJob } from "../../server/persistence/job-repository";
export function JobProgress({ initial }: { initial: StoredJob }) { const [job, setJob] = useState(initial); useEffect(() => { if (!["queued", "running"].includes(job.status)) return; const timer = setInterval(async () => { const response = await fetch(`/api/jobs/${job.id}`, { cache: "no-store" }); if (response.ok) setJob(await response.json()); }, 1000); return () => clearInterval(timer); }, [job.id, job.status]); return <section className="card"><span className={`badge job-${job.status}`}>{job.status}</span><h2>{job.kind.replaceAll("_", " ")}</h2><progress max={job.total || 1} value={job.current} /><p>{job.current} / {job.total}</p><p>{job.currentItem}</p>{job.error && <p role="alert">{job.error}</p>}</section>; }
