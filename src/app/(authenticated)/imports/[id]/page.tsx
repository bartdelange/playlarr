import { connection } from "next/server"; import { notFound } from "next/navigation"; import { database } from "../../../../server/runtime"; import { ImportRepository } from "../../../../server/persistence/import-repository"; import { deleteImport } from "../../../actions/workflows"; import { requestCsrfToken } from "../../../../server/security/request";
export default async function ImportPage({ params }: { params: Promise<{ id: string }> }) { await connection(); const { id } = await params; const repository = new ImportRepository(database); let imported; try { imported = repository.getImport(id); } catch { notFound(); } const entries = repository.entries(id); const csrf = await requestCsrfToken(); return <main><p className="eyebrow">{imported.source}</p><h1>{imported.playlistName}</h1><p><span className="badge">{imported.workflowState.replaceAll("_", " ")}</span> · {entries.length} tracks</p>{imported.lastError && <p role="alert">{imported.lastError}</p>}<div className="table-wrap"><table><thead><tr><th>#</th><th>State</th><th>Track</th><th>Album</th><th>Review</th></tr></thead><tbody>{entries.map((entry) => <tr key={entry.id}><td>{entry.position + 1}</td><td>{entry.resolutionState.replaceAll("_", " ")}</td><td><strong>{entry.track.title}</strong><small>{entry.track.artists.join(", ")}</small></td><td>{entry.track.album}</td><td><a href={`/entries/${entry.id}/review`}>Review</a></td></tr>)}</tbody></table></div><form action={deleteImport}><input type="hidden" name="csrf_token" value={csrf} /><input type="hidden" name="import_id" value={id} /><button className="danger">Delete import</button></form></main>; }
