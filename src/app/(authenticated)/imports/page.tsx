import Link from "next/link"; import { connection } from "next/server"; import { database } from "../../../server/runtime"; import { ImportRepository } from "../../../server/persistence/import-repository";
export default async function ImportsPage() { await connection(); const imports = new ImportRepository(database).listImports(); return <main><h1>Playlist imports</h1><Link className="button" href="/imports/new">Add playlist</Link><div className="card-list">{imports.map((item) => <Link className="card" key={item.id} href={`/imports/${item.id}`}><span className="badge">{item.source}</span><strong>{item.playlistName}</strong><small>{item.workflowState.replaceAll("_", " ")}</small></Link>)}</div></main>; }
