import { expect, it } from "vitest"; import { isrcPattern, marked, nameKey, searchTitle, uniqueValues, versionPreference, words } from "../src/server/integrations/musicbrainz/matching";
it("normalizes MusicBrainz search inputs without discarding version safety signals", () => { expect(isrcPattern.test("USABC1234567")).toBe(true); expect(searchTitle("Song (feat. Guest)")).toBe("Song"); expect([...words("The Song (Radio Edit)")]).toEqual(["song"]); expect(nameKey("Beyoncé & Co.")).toBe("beyoncco"); expect(uniqueValues(["a", "a", undefined, "b"])).toEqual(["a", "b"]); expect(marked("Song (Extended Mix)")).toBe(true); expect(versionPreference("Song (Extended Mix)")).toBeGreaterThan(versionPreference("Song (Radio Edit)")); });
