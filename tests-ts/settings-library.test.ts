import { mkdtempSync, rmSync } from "node:fs"; import { tmpdir } from "node:os"; import path from "node:path"; import { afterEach, expect, it } from "vitest";
import { openDatabase } from "../src/server/persistence/database"; import { ImportRepository } from "../src/server/persistence/import-repository"; import { LibraryRepository } from "../src/server/persistence/library-repository"; import { SettingsRepository } from "../src/server/persistence/settings-repository";
const directories: string[] = []; afterEach(() => directories.splice(0).forEach((directory) => rmSync(directory, { recursive: true })));
it("persists settings, library state, and exports without losing import state", () => { const directory = mkdtempSync(path.join(tmpdir(), "playlarr-library-")); directories.push(directory); const database = openDatabase(path.join(directory, "music-importer.db")); const imports = new ImportRepository(database); const imported = imports.createImport({ source: "spotify", id: "mix", name: "Mix" }); imports.replaceTracks(imported.id, [{ source: "spotify", sourceTrackId: "song", title: "Song", artists: ["Artist"], album: "Album" }]); const settings = new SettingsRepository(database); settings.set("services", { lidarrUrl: "http://lidarr" }); expect(settings.get("services", {})).toEqual({ lidarrUrl: "http://lidarr" }); const library = new LibraryRepository(database); library.saveStatus(imported.id, [{ position: 0, classification: "downloaded", path: "/music/song.flac" }]); const exportId = library.recordExport(imported.id, "/playlists/mix.m3u8", 1, 0); expect(exportId).toBeTruthy(); expect(imports.getImport(imported.id).workflowState).toBe("playlist_generated"); database.close(); });
