import { mkdtempSync, rmSync } from "node:fs"; import { tmpdir } from "node:os"; import path from "node:path"; import { afterEach, expect, it } from "vitest";
import { openDatabase } from "../src/server/persistence/database"; import { ImportRepository } from "../src/server/persistence/import-repository"; import { LidarrPlanRepository } from "../src/server/persistence/lidarr-plan-repository";
const directories: string[] = []; afterEach(() => directories.splice(0).forEach((directory) => rmSync(directory, { recursive: true })));
it("requires explicit approval and supersedes an earlier draft plan", () => { const directory = mkdtempSync(path.join(tmpdir(), "playlarr-plan-")); directories.push(directory); const database = openDatabase(path.join(directory, "music-importer.db")); const imported = new ImportRepository(database).createImport({ source: "spotify", id: "mix", name: "Mix" }); const plans = new LidarrPlanRepository(database); const first = plans.save(imported.id, { actions: [{ action: "create_artist", artistMbid: "artist" }] }); const second = plans.save(imported.id, { actions: [{ action: "queue_search" }] }); expect(plans.get(first).status).toBe("superseded"); plans.approve(second); expect(plans.get(second)).toMatchObject({ status: "approved", plan: { actions: [{ action: "queue_search" }] } }); expect(() => plans.approve(first)).toThrow("only a current draft"); database.close(); });
