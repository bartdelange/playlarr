import { mkdtempSync, rmSync } from "node:fs"; import { tmpdir } from "node:os"; import path from "node:path"; import { afterEach, expect, it } from "vitest";
import { openDatabase } from "../src/server/persistence/database"; import { ImportRepository } from "../src/server/persistence/import-repository"; import { ResolutionRepository } from "../src/server/persistence/resolution-repository";
const directories: string[] = []; afterEach(() => directories.splice(0).forEach((directory) => rmSync(directory, { recursive: true })));
it("keeps manual mappings durable and supersedes draft plans when safety-relevant evidence changes", () => { const directory = mkdtempSync(path.join(tmpdir(), "playlarr-resolution-")); directories.push(directory); const database = openDatabase(path.join(directory, "music-importer.db")); const imports = new ImportRepository(database); const imported = imports.createImport({ source: "spotify", id: "mix", name: "Mix" }); imports.replaceTracks(imported.id, [{ source: "spotify", sourceTrackId: "song", title: "Song", artists: ["Artist"], album: "Album" }]); const entry = imports.entries(imported.id)[0]; database.prepare("INSERT INTO lidarr_plans (id, import_id, status, created_at) VALUES ('draft', ?, 'draft', ?)").run(imported.id, new Date().toISOString()); const resolutions = new ResolutionRepository(database); resolutions.saveManual(entry.id, { resolvedVia: "manual_mbid", recordingIds: ["recording"] }, "manual_mbid", "valid"); expect(database.prepare("SELECT status FROM lidarr_plans WHERE id = 'draft'").pluck().get()).toBe("superseded"); resolutions.setVariousArtistsOverride(entry.id, true); expect(database.prepare("SELECT evidence_json FROM resolutions WHERE entry_id = ?").pluck().get(entry.id)).toContain("allow_various_artists_release"); database.close(); });
it("does not let automation replace a manual mapping and replaces candidate snapshots", () => { const directory = mkdtempSync(path.join(tmpdir(), "playlarr-resolution-")); directories.push(directory); const database = openDatabase(path.join(directory, "music-importer.db")); const imports = new ImportRepository(database); const imported = imports.createImport({ source: "spotify", id: "mix", name: "Mix" }); imports.replaceTracks(imported.id, [{ source: "spotify", sourceTrackId: "song", title: "Song", artists: ["Artist"], album: "Album" }]); const entry = imports.entries(imported.id)[0]; const resolutions = new ResolutionRepository(database); expect(resolutions.saveAutomatic(entry.id, { resolvedVia: "isrc", recordingIds: ["automatic"] })).toBe(true); resolutions.saveCandidates(entry.id, [{ score: 0.4 }, { score: 0.9 }]); resolutions.saveCandidates(entry.id, [{ score: 1 }]); expect(database.prepare("SELECT count(*) FROM resolution_candidates WHERE entry_id = ?").pluck().get(entry.id)).toBe(1); resolutions.saveManual(entry.id, { resolvedVia: "manual_mbid" }, "manual_mbid", "valid"); expect(resolutions.saveAutomatic(entry.id, { resolvedVia: "search" })).toBe(false); database.close(); });
