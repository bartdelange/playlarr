generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model RuntimeMetadata {
  key   String @id
  value String

  @@map("_runtime_metadata")
}
